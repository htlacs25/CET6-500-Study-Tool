# 七天课程重复显示修复（2026-09-07）

本次问题不是9月4日至10日的源课程相同，而是两个显示链路缺陷叠加：浏览器中的错日 `lessonSnapshot` 会覆盖正确课程；`app/dist/client` 又没有进入每日同步，导致本地服务器可能继续提供旧构建。

已完成的修复：

- 错日或内容版本不一致的快照不再参与当天渲染；旧快照、草稿与成绩先归档保留。
- 草稿绑定课程ID和内容指纹，不能跨日期、跨题套用。
- 每套课程生成完整内容指纹；同步检查同时验证日期、整套课程、阅读和听力。
- 根目录、`app/public` 与已有的 `app/dist/client` 同步为同一构建。
- 自动刷新使用带buildId的地址并限制重试次数，避免一次缓存命中后永久停在旧版。
- 启动器在课程检查失败时停止，不再静默打开旧页面。
- 未准备的日期显示明确提示并禁用练习，不再按14天周期循环旧题。
- 已补齐2026年9月11日至30日的20套独立衔接课程；正常日8个新词，9月17日和24日为复盘日。
- 修正复习词调度：`assess` 等历史错词只在D1/D3/D7/D14等到期日优先出现；同日多个到期错词按日期轮换，不再固定每天第一位。
- 课程固定素材版本不再包含动态复习词；新增错词不会误判为“整课更新”。同日课程确实改版时，新旧作答使用不同键，旧成绩和草稿压缩归档并与新题分开统计。

回归检查覆盖：污染七个日期为同一旧快照、连续七日完整课程指纹、全月滚动七日窗口、阅读/听力唯一性、复习词间隔调度、固定课程版本稳定性、新旧题计分隔离、词汇IPA和中文、题目答案索引、长句与翻译规则、三个本地网页副本一致性。浏览器学习记录不会被同步脚本读取或删除。
